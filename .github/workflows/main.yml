name: First Workflow

on: push

jobs:
  builds:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Run a Simple GitHub action
      run: echo "This workflow successfully run"
